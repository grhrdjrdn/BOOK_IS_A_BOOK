  <%= cl_image_tag book.photos.first.key, height: 145, crop: :fill %>
  <h4><%= book.title %></h4>
  <p><%= book.authors %></p>
  <p><%= book.description %></p>
  <p>Current owner: <%= book.current_owner.nickname %></p>
  <p>about <%= book.current_owner.distance_to(current_user).to_f.round(1) %> km away</p>
